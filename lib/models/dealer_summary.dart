class DealerSummary {
  int total_count;
  double total_amount;
  double total_gain;
  double total_collect;
  double total_gain_collect;

  DealerSummary({
    this.total_amount,
    this.total_collect,
    this.total_count,
    this.total_gain,
    this.total_gain_collect,
  });
}
