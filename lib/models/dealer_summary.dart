class DealerSummary {
  int total_count;
  double total_amount;
  double total_gain;
  double total_collect;

  DealerSummary({
    this.total_amount,
    this.total_collect,
    this.total_count,
    this.total_gain,
  });
}
